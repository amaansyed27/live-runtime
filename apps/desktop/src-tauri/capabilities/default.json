{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Live Runtime dashboard window.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
