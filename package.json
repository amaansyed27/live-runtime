{
  "name": "live-runtime",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "dev": "pnpm --filter @live-runtime/dashboard dev",
    "desktop": "pnpm --filter @live-runtime/desktop dev",
    "build": "pnpm -r build",
    "check": "pnpm -r check"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
